python The server is running  on http://localhost:5000 by the command:
 - python app.py

To get the sum of numbers please call the endpoint /total:

    http://localhost:5000/total

The tests could be run by the command:
 - pytest

The server is using python v.3.6

The packages should be installed are listed in the requirements.txt


